type TypeStream = {
  sourceUrl: string;
  streamName: string;
  resolution: string;
  bitrate: string;
  codec: string;
  hwAccel: string;
  logoEnabled: boolean;
  pid: number;
  playlistUrl: string;
};

export type { TypeStream };
